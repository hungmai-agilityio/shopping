import { Mada, Poppins, Roboto, Signika } from 'next/font/google';

export const mada = Mada({
  weight: ['400', '700'],
  subsets: ['latin']
});

export const popping = Poppins({
  subsets: ['latin'],
  weight: ['400', '700']
})

export const roboto = Roboto({
  subsets: ['latin'],
  weight: ['400', '700']
})

export const signika = Signika({
  subsets: ['latin'],
  weight: ['400', '700']
})